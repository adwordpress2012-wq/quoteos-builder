import type { PipPreferences } from '../../types/pip'

export const PIP_STORAGE_KEY = 'quoteos.pip.preferences'

export const PIP_DEFAULT_PREFERENCES: PipPreferences = {
  displayMode: 'full',
  minimized: false,
}

export const PIP_PROFILE_SETTING_KEY = 'pip_preferences'
